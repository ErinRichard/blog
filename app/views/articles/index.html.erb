<h1>Articles</h1>

<ul>
  <%# Controller instance variables (i.e. @articles) can be accessed by the view %>
  <% @articles.each do |article| %>
    <li>
      <%= article.title %>
    </li>
  <% end %>
</ul>
